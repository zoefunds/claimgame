{
  "name": "claimgame",
  "private": true,
  "version": "0.1.0",
  "description": "Put money behind your interpretation of a protocol.",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "verify": "./scripts/run-all-checks.sh"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  },
  "pnpm": {
    "overrides": {
      "@wagmi/connectors": "6.2.0"
    }
  }
}
